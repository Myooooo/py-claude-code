# API客户端
openai>=1.30.0
httpx>=0.27.0

# Token计算
tiktoken>=0.7.0

# 数据验证
pydantic>=2.0.0
pydantic-settings>=2.0.0

# CLI框架
typer>=0.12.0

# UI美化
rich>=13.0.0

# 其他依赖
python-dotenv>=1.0.0

# Web工具（可选，内网环境可能不需要）
# 这些依赖在无法联网安装时可以使用本地镜像或跳过
# beautifulsoup4>=4.12.0  # HTML解析（当前使用内置正则实现，无需此依赖）
# markdownify>=0.11.0     # HTML转Markdown（当前使用内置实现，无需此依赖）
