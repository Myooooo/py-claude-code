#!/usr/bin/env python3
"""
根据Gemini审查意见修复代码问题
"""

FIXES = """
根据Gemini代码审查报告（评分85/100），需要修复以下问题：

## 高优先级修复

### 1. FileReadTool 大文件内存问题
**问题**: `path.read_text()` 会加载整个文件到内存，GB级文件会导致OOM
**修复**: 使用逐行读取 + itertools.islice 实现

### 2. FileEditTool 全局替换副作用
**问题**: `content.replace()` 替换所有匹配，可能产生意外副作用
**修复**: 添加 `count` 参数，默认只替换第一次匹配

### 3. FileWriteTool 原子写入
**问题**: 写入失败可能导致原文件损坏
**修复**: 使用"临时文件 + 重命名"的原子写入策略

## 中优先级修复

### 4. BashTool 安全加固
**问题**: 字符串匹配可被绕过（如 `rm -rf /` 通过环境变量）
**修复**: 使用正则表达式 + 分词检查，增加更全面的危险命令检测

### 5. GrepTool 性能优化
**问题**: `rglob("*")` 遍历所有文件再过滤，性能差
**修复**: 尽早应用glob过滤，使用生成器模式

## 低优先级改进

### 6. ToolResult.data 类型细化
**问题**: `dict[str, Any]` 类型过于松散
**修复**: 使用 TypedDict 定义具体结构

请依次修复这些问题，保持代码质量和类型注解的完整性。
"""

if __name__ == "__main__":
    print(FIXES)
